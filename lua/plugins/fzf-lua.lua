-- this currently doesn't appear to do anything
return {
  "ibhagwan/fzf-lua",
  opts = {
    fzf_colors = true,
    defaults = {
      hl = {
        current = "FzfLuaSelection", -- A custom highlight group
      },
    },
  },
}
